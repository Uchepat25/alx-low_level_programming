#include <stdio.h>
/**
 * main - Entry point
 * avoid:parameter
 * Return: 0 (success)
 */
int main(void)
{
	int;
	for(i=97;i<123;i++)
	{
		if(i==101||1==113)
		{
			continue;
		}
		else
		{
			putchar(i);
		}
	}
	putchar(10);
	return (0);
}
